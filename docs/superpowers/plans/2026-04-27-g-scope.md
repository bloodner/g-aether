# G-Scope Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a mode-status strip to the floating gadget, rename "Live Telemetry" to "G-Scope", and give the panel its own customization (visible tiles, accent, size, history window) — settings independent from the gadget.

**Architecture:** Lift the four mode-badge property triples out of `MainViewModel` into a new `ModeStripViewModel` so both the main window strip and the gadget strip read from one source. Add a parallel `ScopeService`/`ScopeSettingsViewModel` pair (mirror of `GadgetService` + the gadget settings on `ExtraSettingsViewModel`) that drives a new `ScopeSettingsWindow` reachable from a gear icon in the panel header. Extend `MonitorViewModel` rolling buffers from 60 to 900 samples and expose `SparklineHeight` / `ValueFontSize` / `AccentBrushFor` / `HistoryWindowSeconds` for the panel to bind against.

**Tech Stack:** WPF (XAML + C#), CommunityToolkit.Mvvm, .NET 8, the existing `AppConfig` JSON store.

**Spec:** `docs/superpowers/specs/2026-04-27-g-scope-design.md`

---

## File Structure

| File | Action | Responsibility |
|------|--------|---------------|
| `app/GHelper.WPF/ViewModels/ModeStripViewModel.cs` | Create | Owns Perf/Gpu/Display/Services badge name/brush/warning. Subscribes to child VM changes. |
| `app/GHelper.WPF/ViewModels/MainViewModel.cs` | Modify | Delegate badge logic to `ModeStripViewModel`; expose as `ModeStrip` property. |
| `app/GHelper.WPF/Views/MainWindow.xaml` | Modify | Update badge bindings from `{Binding PerfBadgeName}` → `{Binding ModeStrip.PerfBadgeName}`, etc. |
| `app/GHelper.WPF/Views/GadgetWindow.xaml` | Modify | Add a mode-strip row between header and tile grid. |
| `app/GHelper.WPF/Views/GadgetWindow.xaml.cs` | Modify | Apply `gadget_show_modestrip`; recompute height with strip. |
| `app/GHelper.WPF/Services/GadgetService.cs` | Modify | `Configure` takes both `MonitorViewModel` and `ModeStripViewModel`; gadget DataContext exposes both. |
| `app/GHelper.WPF/Views/MainWindow.xaml.cs` | Modify | Pass both VMs into `GadgetService.Configure` and call `ScopeService.Configure`. |
| `app/GHelper.WPF/Views/GadgetSettingsWindow.xaml` | Modify | Retitle to "G-Scope Floating Settings"; add "Show mode strip" toggle. |
| `app/GHelper.WPF/ViewModels/ExtraSettingsViewModel.cs` | Modify | Add `ShowGadgetModeStrip` observable + handler. |
| `app/GHelper.WPF/Views/Panels/MonitorPanel.xaml` | Modify | Rename header to "G-Scope"; add gear button; bind `Height`/`ValueFontSize`/`StrokeColor`/`Visibility` to scope properties. |
| `app/GHelper.WPF/Views/Panels/MonitorPanel.xaml.cs` | Modify | Click handler for the new gear button. |
| `app/GHelper.WPF/ViewModels/MonitorViewModel.cs` | Modify | Buffers grow to 900; add `SparklineHeight`, `ValueFontSize`, accent brushes, visibility booleans, `HistoryWindowSeconds`, `ReloadScopeSettings()`. |
| `app/GHelper.WPF/ViewModels/ScopeSettingsViewModel.cs` | Create | All `Scope*` properties + change handlers writing to `AppConfig` + calling `ScopeService.ApplySettings()`. |
| `app/GHelper.WPF/Services/ScopeService.cs` | Create | Static service: `Configure(MonitorViewModel)`, `ApplySettings()`. |
| `app/GHelper.WPF/Views/ScopeSettingsWindow.xaml` | Create | Modal settings window styled like `GadgetSettingsWindow`. |
| `app/GHelper.WPF/Views/ScopeSettingsWindow.xaml.cs` | Create | Code-behind: drag, close, static `Show(owner)`. |
| `README.md` | Modify | Rename "Live Telemetry" → "G-Scope", "Floating Gadget" → "G-Scope Floating" (keeping casual phrase). |

## Build & smoke-test pattern

This project has no test framework. Each task ends with a build + visual smoke test:

```bash
# Kill any running instance (bash on Windows uses double-slash for taskkill flags)
taskkill //F //IM "G-Aether.exe" 2>/dev/null || true

# Build (must report 0 errors)
dotnet build app/GHelper.WPF/GHelper.WPF.csproj

# Run (then perform the visual check described in each task)
dotnet run --project app/GHelper.WPF/GHelper.WPF.csproj
```

After each task, commit with a focused message. Frequent commits make rollback safe.

---

## Task 1: Create ModeStripViewModel

**Files:**
- Create: `app/GHelper.WPF/ViewModels/ModeStripViewModel.cs`

- [ ] **Step 1: Create the new file**

```csharp
// app/GHelper.WPF/ViewModels/ModeStripViewModel.cs
using System.Windows.Media;
using CommunityToolkit.Mvvm.ComponentModel;
using GHelper.WPF.Services;

namespace GHelper.WPF.ViewModels
{
    /// <summary>
    /// Single source of truth for the four mode badges (Performance, GPU,
    /// Display, Services). Both the main window's strip and the gadget's
    /// strip bind to these properties so any change applies in lockstep.
    /// Owned by MainViewModel; subscribes to its child VMs to recompute.
    /// </summary>
    public partial class ModeStripViewModel : ObservableObject
    {
        private readonly PerformanceViewModel _performance;
        private readonly GpuViewModel _gpu;
        private readonly VisualModeViewModel _visual;
        private readonly ExtraSettingsViewModel _extra;

        [ObservableProperty] private string _perfBadgeName = "Balanced";
        [ObservableProperty] private Brush _perfBadgeBrush =
            new SolidColorBrush(Color.FromRgb(0x60, 0xCD, 0xFF));
        [ObservableProperty] private bool _perfBadgeIsWarning;

        [ObservableProperty] private string _gpuBadgeName = "Standard";
        [ObservableProperty] private Brush _gpuBadgeBrush =
            new SolidColorBrush(Color.FromRgb(0x60, 0xCD, 0xFF));
        [ObservableProperty] private bool _gpuBadgeIsWarning;

        [ObservableProperty] private string _displayBadgeName = "Auto";
        [ObservableProperty] private Brush _displayBadgeBrush =
            new SolidColorBrush(Color.FromRgb(0x60, 0xCD, 0xFF));

        [ObservableProperty] private string _servicesBadgeName = "Healthy";
        [ObservableProperty] private Brush _servicesBadgeBrush =
            new SolidColorBrush(Color.FromRgb(0x4C, 0xC9, 0x5E));
        [ObservableProperty] private bool _servicesBadgeIsWarning;

        // Header accent hairline — green when healthy, orange/red when any badge is warning.
        [ObservableProperty] private Brush _headerAccentBrush =
            new SolidColorBrush(Color.FromRgb(0x4C, 0xC9, 0x5E));

        private static Brush ModeBrush(Color c) => new SolidColorBrush(c);

        public ModeStripViewModel(
            PerformanceViewModel performance,
            GpuViewModel gpu,
            VisualModeViewModel visual,
            ExtraSettingsViewModel extra)
        {
            _performance = performance;
            _gpu = gpu;
            _visual = visual;
            _extra = extra;

            _performance.PropertyChanged += (_, e) =>
            {
                if (e.PropertyName == nameof(PerformanceViewModel.SelectedModeIndex) ||
                    e.PropertyName == nameof(PerformanceViewModel.ModeLabels))
                    UpdatePerfBadge();
            };
            _gpu.PropertyChanged += (_, e) =>
            {
                if (e.PropertyName == nameof(GpuViewModel.SelectedModeIndex) ||
                    e.PropertyName == nameof(GpuViewModel.ModeLabels) ||
                    e.PropertyName == nameof(GpuViewModel.ModeColors))
                    UpdateGpuBadge();
            };
            _visual.PropertyChanged += (_, e) =>
            {
                if (e.PropertyName == nameof(VisualModeViewModel.SelectedFreqIndex) ||
                    e.PropertyName == nameof(VisualModeViewModel.FrequencyLabels) ||
                    e.PropertyName == nameof(VisualModeViewModel.IsAutoFreqMode))
                    UpdateDisplayBadge();
            };
            _extra.PropertyChanged += (_, e) =>
            {
                if (e.PropertyName == nameof(ExtraSettingsViewModel.AsusServicesCount) ||
                    e.PropertyName == nameof(ExtraSettingsViewModel.AsusServicesText))
                    UpdateServicesBadge();
            };

            UpdatePerfBadge();
            UpdateGpuBadge();
            UpdateDisplayBadge();
            UpdateServicesBadge();
        }

        private void UpdatePerfBadge()
        {
            int idx = _performance.SelectedModeIndex;
            var labels = _performance.ModeLabels;
            if (idx < 0 || idx >= labels.Length) return;

            string name = labels[idx];
            PerfBadgeName = name;
            PerfBadgeBrush = name switch
            {
                "Silent" => ModeBrush(ThemeService.ColorSilent),
                "Turbo" => ModeBrush(ThemeService.ColorTurbo),
                _ => ModeBrush(ThemeService.ColorBalanced),
            };
            PerfBadgeIsWarning = name == "Turbo";
            UpdateHeaderAccent();
        }

        private void UpdateGpuBadge()
        {
            int idx = _gpu.SelectedModeIndex;
            var labels = _gpu.ModeLabels;
            if (idx < 0 || idx >= labels.Length) return;

            string name = labels[idx];
            GpuBadgeName = name;
            GpuBadgeBrush = name switch
            {
                "Eco" => ModeBrush(ThemeService.ColorEco),
                "Ultimate" => ModeBrush(ThemeService.ColorUltimate),
                "Optimized" => ModeBrush(ThemeService.ColorOptimized),
                _ => ModeBrush(ThemeService.ColorStandard),
            };
            GpuBadgeIsWarning = name == "Ultimate";
            UpdateHeaderAccent();
        }

        private void UpdateDisplayBadge()
        {
            int idx = _visual.SelectedFreqIndex;
            var labels = _visual.FrequencyLabels;
            if (_visual.IsAutoFreqMode)
                DisplayBadgeName = "Auto";
            else if (idx >= 0 && idx < labels.Length)
                DisplayBadgeName = labels[idx];

            DisplayBadgeBrush = _visual.IsAutoFreqMode
                ? ModeBrush(ThemeService.ColorBalanced)
                : ModeBrush(ThemeService.ColorOptimized);
            UpdateHeaderAccent();
        }

        private void UpdateServicesBadge()
        {
            int count = _extra.AsusServicesCount;
            if (count <= 0)
            {
                ServicesBadgeName = "Healthy";
                ServicesBadgeBrush = ModeBrush(ThemeService.ColorEco);
                ServicesBadgeIsWarning = false;
            }
            else
            {
                ServicesBadgeName = count == 1 ? "1 Running" : $"{count} Running";
                ServicesBadgeBrush = ModeBrush(ThemeService.ColorTurbo);
                ServicesBadgeIsWarning = true;
            }
            UpdateHeaderAccent();
        }

        private void UpdateHeaderAccent()
        {
            bool anyWarning = ServicesBadgeIsWarning || PerfBadgeIsWarning || GpuBadgeIsWarning;
            HeaderAccentBrush = anyWarning
                ? ModeBrush(ThemeService.ColorTurbo)
                : ModeBrush(ThemeService.ColorEco);
        }

        /// <summary>
        /// Allows MainViewModel's sensor tick to ask for a forced refresh
        /// (e.g., after a hotkey-triggered mode change) without exposing
        /// every Update*Badge method individually.
        /// </summary>
        public void Refresh()
        {
            UpdatePerfBadge();
            UpdateGpuBadge();
            UpdateDisplayBadge();
            UpdateServicesBadge();
        }
    }
}
```

- [ ] **Step 2: Build to confirm the new file compiles in isolation**

```bash
dotnet build app/GHelper.WPF/GHelper.WPF.csproj
```

Expected: 0 errors. Warnings about the unused class are fine — it's wired up in Task 2.

- [ ] **Step 3: Commit**

```bash
git add app/GHelper.WPF/ViewModels/ModeStripViewModel.cs
git commit -m "feat: add ModeStripViewModel as single source for badge state"
```

---

## Task 2: Wire ModeStripViewModel into MainViewModel

**Files:**
- Modify: `app/GHelper.WPF/ViewModels/MainViewModel.cs`
- Modify: `app/GHelper.WPF/Views/MainWindow.xaml`

- [ ] **Step 1: Replace badge properties + helpers with a `ModeStrip` reference**

In `MainViewModel.cs`:

**Remove** lines 47–69 (the `// Mode Badge Strip` block — `_perfBadgeName`, `_perfBadgeBrush`, `_perfBadgeIsWarning`, `_gpuBadgeName`, `_gpuBadgeBrush`, `_gpuBadgeIsWarning`, `_displayBadgeName`, `_displayBadgeBrush`, `_servicesBadgeName`, `_servicesBadgeBrush`, `_servicesBadgeIsWarning`, `_headerAccentBrush`, and `ModeBrush`).

**Add** a `ModeStrip` observable property in the same place:

```csharp
[ObservableProperty]
private ModeStripViewModel _modeStrip = null!;
```

(The `null!` is fine — it's set in the constructor before any binding evaluates.)

**Remove** the four `Update*Badge` methods (lines 264–341, the entire block ending with `UpdateHeaderAccent`).

**Replace** the four PropertyChanged subscriptions (lines 127–152) and the trailing four direct `Update*Badge()` calls (lines 154–157) with a single line *after* the child VMs are initialized:

```csharp
// Mode Badge Strip — owned by ModeStripViewModel; sources from child VMs.
ModeStrip = new ModeStripViewModel(Performance, Gpu, Visual, Extra);
```

Place that line right after `FansPower.Initialize();` (around current line 124) and before the timer setup.

- [ ] **Step 2: Update MainWindow.xaml bindings**

In `app/GHelper.WPF/Views/MainWindow.xaml`, the four mode-badge buttons currently bind like `{Binding PerfBadgeName}`, `{Binding PerfBadgeBrush}`, etc. Each binding gains a `ModeStrip.` prefix.

Concretely, every occurrence of these property names inside that file gets prefixed:

| Before | After |
|---|---|
| `{Binding PerfBadgeName}` | `{Binding ModeStrip.PerfBadgeName}` |
| `{Binding PerfBadgeBrush}` | `{Binding ModeStrip.PerfBadgeBrush}` |
| `{Binding PerfBadgeIsWarning}` | `{Binding ModeStrip.PerfBadgeIsWarning}` |
| `{Binding GpuBadgeName}` | `{Binding ModeStrip.GpuBadgeName}` |
| `{Binding GpuBadgeBrush}` | `{Binding ModeStrip.GpuBadgeBrush}` |
| `{Binding GpuBadgeIsWarning}` | `{Binding ModeStrip.GpuBadgeIsWarning}` |
| `{Binding DisplayBadgeName}` | `{Binding ModeStrip.DisplayBadgeName}` |
| `{Binding DisplayBadgeBrush}` | `{Binding ModeStrip.DisplayBadgeBrush}` |
| `{Binding ServicesBadgeName}` | `{Binding ModeStrip.ServicesBadgeName}` |
| `{Binding ServicesBadgeBrush}` | `{Binding ModeStrip.ServicesBadgeBrush}` |
| `{Binding ServicesBadgeIsWarning}` | `{Binding ModeStrip.ServicesBadgeIsWarning}` |
| `{Binding HeaderAccentBrush}` | `{Binding ModeStrip.HeaderAccentBrush}` |

Use Grep to find every occurrence and Edit to update each.

- [ ] **Step 3: Build**

```bash
dotnet build app/GHelper.WPF/GHelper.WPF.csproj
```

Expected: 0 errors.

- [ ] **Step 4: Smoke test**

```bash
taskkill //F //IM "G-Aether.exe" 2>/dev/null || true
dotnet run --project app/GHelper.WPF/GHelper.WPF.csproj
```

Visual check:
- Main window's badge strip displays correctly (PERF / GPU / DISPLAY / SERVICES with the right names and colors).
- Switch performance modes via the slider — Perf badge updates live.
- Switch GPU modes — GPU badge updates live.
- Header hairline color reflects overall health.

- [ ] **Step 5: Commit**

```bash
git add app/GHelper.WPF/ViewModels/MainViewModel.cs app/GHelper.WPF/Views/MainWindow.xaml
git commit -m "refactor: route MainWindow badges through ModeStripViewModel"
```

---

## Task 3: Add the mode strip row to GadgetWindow XAML

**Files:**
- Modify: `app/GHelper.WPF/Views/GadgetWindow.xaml`

- [ ] **Step 1: Add a new row between header and tile grid**

In `GadgetWindow.xaml`, the `Grid` inside the outer `Border` currently has two row definitions. Update them to three, and insert the strip row.

**Replace** the existing `<Grid.RowDefinitions>` block:

```xml
<Grid.RowDefinitions>
    <RowDefinition Height="Auto" />
    <RowDefinition Height="*" />
</Grid.RowDefinitions>
```

**With:**

```xml
<Grid.RowDefinitions>
    <RowDefinition Height="Auto" />
    <RowDefinition Height="Auto" />
    <RowDefinition Height="*" />
</Grid.RowDefinitions>
```

**Update** the `UniformGrid x:Name="TileGrid"` `Grid.Row="1"` attribute to `Grid.Row="2"`.

**Insert** this new mode strip block between the header `Grid` (Row 0) and the `UniformGrid` (now Row 2):

```xml
<!-- Mode strip — mini of the main window's PERF/GPU/DISPLAY/SERVICES strip.
     Bound to the same ModeStripViewModel instance the main window uses, so
     names/colors/warnings always match. Hairline dividers above and below. -->
<Border x:Name="ModeStripContainer"
        Grid.Row="1"
        Margin="0,0,0,8"
        Padding="2,4,2,4"
        BorderBrush="#18FFFFFF"
        BorderThickness="0,1,0,1">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Perf badge -->
        <StackPanel Grid.Column="0" Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    ToolTip="{Binding ModeStrip.PerfBadgeName, StringFormat='Performance: {0}'}">
            <TextBlock Text="&#xE945;"
                       Style="{StaticResource ModeBadgePulseIconStyle}"
                       Foreground="{Binding ModeStrip.PerfBadgeBrush}"
                       IsEnabled="{Binding ModeStrip.PerfBadgeIsWarning}" />
            <TextBlock Text="{Binding ModeStrip.PerfBadgeName}"
                       Style="{StaticResource ModeBadgeValueStyle}"
                       Foreground="{Binding ModeStrip.PerfBadgeBrush}" />
        </StackPanel>

        <!-- GPU badge -->
        <StackPanel Grid.Column="1" Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    ToolTip="{Binding ModeStrip.GpuBadgeName, StringFormat='GPU: {0}'}">
            <TextBlock Text="&#xE950;"
                       Style="{StaticResource ModeBadgePulseIconStyle}"
                       Foreground="{Binding ModeStrip.GpuBadgeBrush}"
                       IsEnabled="{Binding ModeStrip.GpuBadgeIsWarning}" />
            <TextBlock Text="{Binding ModeStrip.GpuBadgeName}"
                       Style="{StaticResource ModeBadgeValueStyle}"
                       Foreground="{Binding ModeStrip.GpuBadgeBrush}" />
        </StackPanel>

        <!-- Display badge -->
        <StackPanel Grid.Column="2" Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    ToolTip="{Binding ModeStrip.DisplayBadgeName, StringFormat='Display: {0}'}">
            <TextBlock Text="&#xE7F4;"
                       Style="{StaticResource ModeBadgeIconStyle}"
                       Foreground="{Binding ModeStrip.DisplayBadgeBrush}" />
            <TextBlock Text="{Binding ModeStrip.DisplayBadgeName}"
                       Style="{StaticResource ModeBadgeValueStyle}"
                       Foreground="{Binding ModeStrip.DisplayBadgeBrush}" />
        </StackPanel>

        <!-- Services badge -->
        <StackPanel Grid.Column="3" Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    ToolTip="{Binding ModeStrip.ServicesBadgeName, StringFormat='Services: {0}'}">
            <TextBlock Text="&#xE72E;"
                       Style="{StaticResource ModeBadgePulseIconStyle}"
                       Foreground="{Binding ModeStrip.ServicesBadgeBrush}"
                       IsEnabled="{Binding ModeStrip.ServicesBadgeIsWarning}" />
            <TextBlock Text="{Binding ModeStrip.ServicesBadgeName}"
                       Style="{StaticResource ModeBadgeValueStyle}"
                       Foreground="{Binding ModeStrip.ServicesBadgeBrush}" />
        </StackPanel>
    </Grid>
</Border>
```

The `x:Name="ModeStripContainer"` is required — Task 4 toggles its `Visibility` from code-behind.

- [ ] **Step 2: Build (will not yet display anything because DataContext lacks `ModeStrip`)**

```bash
dotnet build app/GHelper.WPF/GHelper.WPF.csproj
```

Expected: 0 errors. Binding warnings about `ModeStrip` not resolving are expected — Task 4 wires that up.

- [ ] **Step 3: Commit**

```bash
git add app/GHelper.WPF/Views/GadgetWindow.xaml
git commit -m "feat: add mode-strip row to gadget window XAML"
```

---

## Task 4: Wire mode strip data into GadgetService and GadgetWindow

**Files:**
- Modify: `app/GHelper.WPF/Services/GadgetService.cs`
- Modify: `app/GHelper.WPF/Views/GadgetWindow.xaml.cs`
- Modify: `app/GHelper.WPF/Views/MainWindow.xaml.cs`

- [ ] **Step 1: Replace GadgetService with the two-VM version**

The gadget's bindings need to resolve both `Monitor.*` (tile values) and `ModeStrip.*` (badges) off the same DataContext. Use a thin wrapper class with two properties — bindings on `Monitor.CpuTempText` resolve via the wrapper and propagate `INotifyPropertyChanged` correctly because the original `MonitorViewModel` instance is exposed directly.

Replace the entire contents of `app/GHelper.WPF/Services/GadgetService.cs`:

```csharp
// app/GHelper.WPF/Services/GadgetService.cs
using System.Windows;
using GHelper.WPF.ViewModels;
using GHelper.WPF.Views;

namespace GHelper.WPF.Services
{
    public static class GadgetService
    {
        private const string ConfigKey = "gadget_enabled";

        private static GadgetWindow? _window;
        private static MonitorViewModel? _monitor;
        private static ModeStripViewModel? _modeStrip;

        /// <summary>
        /// DataContext exposed to GadgetWindow. Bindings reach Monitor.*
        /// for tile values and ModeStrip.* for the mode strip.
        /// </summary>
        public sealed class GadgetDataContext
        {
            public MonitorViewModel Monitor { get; }
            public ModeStripViewModel ModeStrip { get; }

            public GadgetDataContext(MonitorViewModel monitor, ModeStripViewModel modeStrip)
            {
                Monitor = monitor;
                ModeStrip = modeStrip;
            }
        }

        public static void Configure(MonitorViewModel monitor, ModeStripViewModel modeStrip)
        {
            _monitor = monitor;
            _modeStrip = modeStrip;
            if (AppConfig.Is(ConfigKey)) Show();
        }

        public static void SetEnabled(bool enabled)
        {
            AppConfig.Set(ConfigKey, enabled ? 1 : 0);
            if (enabled) Show();
            else Hide();
        }

        public static bool IsEnabled => AppConfig.Is(ConfigKey);

        public static void ApplySettings() => _window?.ApplySettings();

        public static void ResetPosition()
        {
            AppConfig.Set("gadget_x", 0);
            AppConfig.Set("gadget_y", 0);
            if (_window != null)
            {
                Hide();
                if (IsEnabled) Show();
            }
        }

        private static void Show()
        {
            if (_window != null || _monitor == null || _modeStrip == null) return;
            _window = new GadgetWindow
            {
                DataContext = new GadgetDataContext(_monitor, _modeStrip),
            };
            _window.Closed += (_, _) => _window = null;
            _window.Show();
        }

        private static void Hide()
        {
            _window?.Close();
            _window = null;
        }
    }
}
```

- [ ] **Step 2: Update GadgetWindow.xaml tile bindings to use the `Monitor.` prefix**

The existing tile bindings (e.g., `{Binding CpuTempText}`) used to resolve directly because the DataContext was `MonitorViewModel`. Now that the DataContext is `GadgetDataContext`, every tile binding needs the `Monitor.` prefix.

In `GadgetWindow.xaml`, rewrite the tile-grid bindings:

| Before | After |
|---|---|
| `{Binding CpuTempText}` | `{Binding Monitor.CpuTempText}` |
| `{Binding GpuTempText}` | `{Binding Monitor.GpuTempText}` |
| `{Binding CpuUseText}` | `{Binding Monitor.CpuUseText}` |
| `{Binding GpuUseText}` | `{Binding Monitor.GpuUseText}` |
| `{Binding GpuPowerText}` | `{Binding Monitor.GpuPowerText}` |
| `{Binding BatteryPercentText}` | `{Binding Monitor.BatteryPercentText}` |
| `{Binding CpuFanCurrentText}` | `{Binding Monitor.CpuFanCurrentText}` |
| `{Binding GpuFanCurrentText}` | `{Binding Monitor.GpuFanCurrentText}` |

Use Grep on `GadgetWindow.xaml` to confirm every tile-text binding has been rewritten — there should be eight.

- [ ] **Step 3: Update MainWindow.xaml.cs to pass both VMs**

In `app/GHelper.WPF/Views/MainWindow.xaml.cs`, find the existing call (around line 29):

```csharp
GadgetService.Configure(mainVm.Monitor);
```

Replace with:

```csharp
GadgetService.Configure(mainVm.Monitor, mainVm.ModeStrip);
```

- [ ] **Step 4: Apply config-driven visibility for the strip in GadgetWindow.ApplySettings**

In `app/GHelper.WPF/Views/GadgetWindow.xaml.cs`, **add** a line inside `ApplySettings()` (anywhere before the size calc — e.g., right after the `TitleText.Visibility` block):

```csharp
// Mode strip visibility — defaults to visible (key absent or 1).
ModeStripContainer.Visibility = AppConfig.Get("gadget_show_modestrip", 1) == 1
    ? Visibility.Visible : Visibility.Collapsed;
```

The strip's contribution to height is automatic via the outer `Grid`'s `RowDefinition Height="Auto"` — `Visibility.Collapsed` removes the row from layout entirely, so no manual height adjustment is needed.

- [ ] **Step 5: Build**

```bash
dotnet build app/GHelper.WPF/GHelper.WPF.csproj
```

Expected: 0 errors.

- [ ] **Step 6: Smoke test**

```bash
taskkill //F //IM "G-Aether.exe" 2>/dev/null || true
dotnet run --project app/GHelper.WPF/GHelper.WPF.csproj
```

Visual checks:
- Open Settings → enable the gadget. Strip appears between the header and the tiles, showing the four current modes (e.g., `⚡ Balanced`, `◆ Standard`, `▣ 144Hz`, `✓ Healthy`).
- Switch performance modes via the slider — gadget's Perf badge updates.
- Hover a badge — tooltip shows the full name (e.g., "Performance: Balanced").
- Tile values still update every second (CPU temp etc.).

- [ ] **Step 7: Commit**

```bash
git add app/GHelper.WPF/Services/GadgetService.cs \
        app/GHelper.WPF/Views/GadgetWindow.xaml \
        app/GHelper.WPF/Views/GadgetWindow.xaml.cs \
        app/GHelper.WPF/Views/MainWindow.xaml.cs
git commit -m "feat: gadget shows live mode strip bound to ModeStripViewModel"
```

---

## Task 5: Add "Show mode strip" toggle and rename gadget settings window

**Files:**
- Modify: `app/GHelper.WPF/ViewModels/ExtraSettingsViewModel.cs`
- Modify: `app/GHelper.WPF/Views/GadgetSettingsWindow.xaml`

- [ ] **Step 1: Add ShowGadgetModeStrip observable + handler**

In `app/GHelper.WPF/ViewModels/ExtraSettingsViewModel.cs`, near the other gadget toggles (e.g., next to `_hideGadgetClose`):

```csharp
[ObservableProperty]
private bool _showGadgetModeStrip = true;
```

Add the change handler near the other `OnHideGadget*Changed` handlers:

```csharp
partial void OnShowGadgetModeStripChanged(bool value)
{
    if (_ignoreChange) return;
    AppConfig.Set("gadget_show_modestrip", value ? 1 : 0);
    GadgetService.ApplySettings();
}
```

In the `Initialize`/load block (around line 666 where `HideGadgetClose = AppConfig.Is(...)` lives), add a line that loads the saved value, defaulting to true:

```csharp
ShowGadgetModeStrip = AppConfig.Get("gadget_show_modestrip", 1) == 1;
```

- [ ] **Step 2: Retitle the gadget settings window and add the new toggle**

In `app/GHelper.WPF/Views/GadgetSettingsWindow.xaml`, find the title bar `TextBlock`:

```xml
<TextBlock Text="Floating Gadget Settings" FontSize="12" FontWeight="SemiBold" ... />
```

Change to:

```xml
<TextBlock Text="G-Scope Floating Settings" FontSize="12" FontWeight="SemiBold" ... />
```

Also update the window's `Title` attribute if present:

```xml
Title="G-Scope Floating Settings"
```

In the `<!-- APPEARANCE -->` card, **add** this `Grid` block right above the existing "Hide close button" row (so the new toggle is the first appearance setting):

```xml
<!-- Mode strip toggle -->
<Grid Margin="0,0,0,12">
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
    </Grid.ColumnDefinitions>
    <StackPanel VerticalAlignment="Center">
        <TextBlock Text="Show mode strip" Style="{StaticResource BodyTextStyle}" />
        <TextBlock Text="Mirrors the main window's Perf/GPU/Display/Services badges"
                   Style="{StaticResource DimTextStyle}" FontSize="10" />
    </StackPanel>
    <controls:ToggleSwitch Grid.Column="1"
                           IsChecked="{Binding ShowGadgetModeStrip, Mode=TwoWay}"
                           Width="44" Height="22" VerticalAlignment="Center" />
</Grid>
```

- [ ] **Step 3: Build**

```bash
dotnet build app/GHelper.WPF/GHelper.WPF.csproj
```

Expected: 0 errors.

- [ ] **Step 4: Smoke test**

```bash
taskkill //F //IM "G-Aether.exe" 2>/dev/null || true
dotnet run --project app/GHelper.WPF/GHelper.WPF.csproj
```

Visual checks:
- Settings panel → open the gadget settings dialog. Title reads **"G-Scope Floating Settings"**.
- The Appearance card now starts with a "Show mode strip" toggle (defaulting on).
- Toggle off → strip disappears from the gadget; toggle on → strip reappears. No app restart needed.
- The setting persists across app restarts.

- [ ] **Step 5: Commit**

```bash
git add app/GHelper.WPF/ViewModels/ExtraSettingsViewModel.cs \
        app/GHelper.WPF/Views/GadgetSettingsWindow.xaml
git commit -m "feat: add 'Show mode strip' toggle; rename to G-Scope Floating Settings"
```

---

## Task 6: Rename "Live Telemetry" to "G-Scope" in the panel and README

**Files:**
- Modify: `app/GHelper.WPF/Views/Panels/MonitorPanel.xaml`
- Modify: `README.md`

- [ ] **Step 1: Rename in the panel header**

In `app/GHelper.WPF/Views/Panels/MonitorPanel.xaml`, find the header text:

```xml
<TextBlock Text="Live Telemetry"
           Style="{StaticResource CardTitleStyle}"
           VerticalAlignment="Center" />
```

Change `Text="Live Telemetry"` to `Text="G-Scope"`.

- [ ] **Step 2: Update README**

Use Grep to find every occurrence of "Live Telemetry" and "Floating Gadget" in `README.md`. For each:

- "Live Telemetry" → "G-Scope"
- "Floating Gadget" (when used as a feature name / heading / first reference) → "G-Scope Floating"
- "Floating Gadget" used casually mid-sentence → leave alone (it's still a friendly synonym)

If a section heading reads `## Live Telemetry, Floating Gadget, and developer pipe` (per the latest commit message), rewrite it as `## G-Scope, G-Scope Floating, and the developer pipe`.

- [ ] **Step 3: Build**

```bash
dotnet build app/GHelper.WPF/GHelper.WPF.csproj
```

Expected: 0 errors.

- [ ] **Step 4: Smoke test**

```bash
taskkill //F //IM "G-Aether.exe" 2>/dev/null || true
dotnet run --project app/GHelper.WPF/GHelper.WPF.csproj
```

Visual check: navigate to the Monitor panel — header reads **"G-Scope"** instead of "Live Telemetry".

- [ ] **Step 5: Commit**

```bash
git add app/GHelper.WPF/Views/Panels/MonitorPanel.xaml README.md
git commit -m "refactor: rename Live Telemetry to G-Scope in user-facing copy"
```

---

## Task 7: Create ScopeSettingsViewModel

**Files:**
- Create: `app/GHelper.WPF/ViewModels/ScopeSettingsViewModel.cs`

- [ ] **Step 1: Create the new file**

```csharp
// app/GHelper.WPF/ViewModels/ScopeSettingsViewModel.cs
using CommunityToolkit.Mvvm.ComponentModel;
using CommunityToolkit.Mvvm.Input;
using GHelper.WPF.Services;

namespace GHelper.WPF.ViewModels
{
    /// <summary>
    /// Backs the G-Scope Settings window. Independent from the gadget's
    /// settings on ExtraSettingsViewModel — Scope and Floating each have
    /// their own AppConfig namespace.
    /// </summary>
    public partial class ScopeSettingsViewModel : ObservableObject
    {
        private bool _ignoreChange;

        // ---- Visible tiles ----
        [ObservableProperty] private bool _showCpuTemp = true;
        [ObservableProperty] private bool _showDgpuTemp = true;
        [ObservableProperty] private bool _showCpuUse = true;
        [ObservableProperty] private bool _showDgpuUse = true;
        [ObservableProperty] private bool _showPower = true;
        [ObservableProperty] private bool _showBattery = true;
        [ObservableProperty] private bool _showCpuFan = true;
        [ObservableProperty] private bool _showGpuFan = true;

        // ---- Accent ----
        [ObservableProperty]
        [NotifyPropertyChangedFor(
            nameof(IsAccentMulti), nameof(IsAccentBlue), nameof(IsAccentPurple),
            nameof(IsAccentGreen), nameof(IsAccentOrange), nameof(IsAccentRed),
            nameof(IsAccentWhite), nameof(IsAccentDark))]
        private string _scopeAccent = "multi";

        public bool IsAccentMulti => ScopeAccent == "multi";
        public bool IsAccentBlue => ScopeAccent == "blue";
        public bool IsAccentPurple => ScopeAccent == "purple";
        public bool IsAccentGreen => ScopeAccent == "green";
        public bool IsAccentOrange => ScopeAccent == "orange";
        public bool IsAccentRed => ScopeAccent == "red";
        public bool IsAccentWhite => ScopeAccent == "white";
        public bool IsAccentDark => ScopeAccent == "dark";

        // ---- Size ----
        [ObservableProperty]
        [NotifyPropertyChangedFor(
            nameof(IsScopeSizeCompact), nameof(IsScopeSizeRegular), nameof(IsScopeSizeLarge))]
        private string _scopeSize = "regular";

        public bool IsScopeSizeCompact => ScopeSize == "compact";
        public bool IsScopeSizeRegular => ScopeSize == "regular";
        public bool IsScopeSizeLarge => ScopeSize == "large";

        // ---- History window ----
        [ObservableProperty]
        [NotifyPropertyChangedFor(
            nameof(IsHistory60s), nameof(IsHistory5min), nameof(IsHistory15min))]
        private string _scopeHistoryWindow = "60s";

        public bool IsHistory60s => ScopeHistoryWindow == "60s";
        public bool IsHistory5min => ScopeHistoryWindow == "5min";
        public bool IsHistory15min => ScopeHistoryWindow == "15min";

        public ScopeSettingsViewModel()
        {
            _ignoreChange = true;
            ShowCpuTemp = AppConfig.Get("scope_show_cpu_temp", 1) == 1;
            ShowDgpuTemp = AppConfig.Get("scope_show_dgpu_temp", 1) == 1;
            ShowCpuUse = AppConfig.Get("scope_show_cpu_use", 1) == 1;
            ShowDgpuUse = AppConfig.Get("scope_show_dgpu_use", 1) == 1;
            ShowPower = AppConfig.Get("scope_show_power", 1) == 1;
            ShowBattery = AppConfig.Get("scope_show_battery", 1) == 1;
            ShowCpuFan = AppConfig.Get("scope_show_cpu_fan", 1) == 1;
            ShowGpuFan = AppConfig.Get("scope_show_gpu_fan", 1) == 1;
            ScopeAccent = AppConfig.GetString("scope_accent") ?? "multi";
            ScopeSize = AppConfig.GetString("scope_size") ?? "regular";
            ScopeHistoryWindow = AppConfig.GetString("scope_history_window") ?? "60s";
            _ignoreChange = false;
        }

        // ---- Visible-tile change handlers ----
        partial void OnShowCpuTempChanged(bool value) => SaveBool("scope_show_cpu_temp", value);
        partial void OnShowDgpuTempChanged(bool value) => SaveBool("scope_show_dgpu_temp", value);
        partial void OnShowCpuUseChanged(bool value) => SaveBool("scope_show_cpu_use", value);
        partial void OnShowDgpuUseChanged(bool value) => SaveBool("scope_show_dgpu_use", value);
        partial void OnShowPowerChanged(bool value) => SaveBool("scope_show_power", value);
        partial void OnShowBatteryChanged(bool value) => SaveBool("scope_show_battery", value);
        partial void OnShowCpuFanChanged(bool value) => SaveBool("scope_show_cpu_fan", value);
        partial void OnShowGpuFanChanged(bool value) => SaveBool("scope_show_gpu_fan", value);

        partial void OnScopeAccentChanged(string value)
        {
            if (_ignoreChange) return;
            AppConfig.Set("scope_accent", value);
            ScopeService.ApplySettings();
        }

        partial void OnScopeSizeChanged(string value)
        {
            if (_ignoreChange) return;
            AppConfig.Set("scope_size", value);
            ScopeService.ApplySettings();
        }

        partial void OnScopeHistoryWindowChanged(string value)
        {
            if (_ignoreChange) return;
            AppConfig.Set("scope_history_window", value);
            ScopeService.ApplySettings();
        }

        private void SaveBool(string key, bool value)
        {
            if (_ignoreChange) return;
            AppConfig.Set(key, value ? 1 : 0);
            ScopeService.ApplySettings();
        }

        [RelayCommand]
        private void SetScopeAccent(string accent) => ScopeAccent = accent;

        [RelayCommand]
        private void SetScopeSize(string size) => ScopeSize = size;

        [RelayCommand]
        private void SetScopeHistory(string window) => ScopeHistoryWindow = window;
    }
}
```

**Note:** Tasks 7, 8, and 9 form a single compile unit. `ScopeSettingsViewModel` references `ScopeService.ApplySettings`; `ScopeService` references `MonitorViewModel.ReloadScopeSettings`. Don't try to build or commit until the end of Task 9 — proceed straight to Task 8.

---

## Task 8: Create ScopeService

**Files:**
- Create: `app/GHelper.WPF/Services/ScopeService.cs`
- Modify: `app/GHelper.WPF/Views/MainWindow.xaml.cs`

- [ ] **Step 1: Create ScopeService**

```csharp
// app/GHelper.WPF/Services/ScopeService.cs
using GHelper.WPF.ViewModels;

namespace GHelper.WPF.Services
{
    /// <summary>
    /// Lifecycle for the G-Scope panel's runtime config. Parallel to
    /// GadgetService but lighter — there's no window to own; the panel is
    /// embedded in the main window. Provides a single ApplySettings() that
    /// MonitorViewModel listens for to re-read its scope_* config keys.
    /// </summary>
    public static class ScopeService
    {
        private static MonitorViewModel? _monitor;

        /// <summary>
        /// Called once from MainWindow after the MainViewModel is built.
        /// Hands the panel's MonitorViewModel a reference so ApplySettings
        /// can poke it later.
        /// </summary>
        public static void Configure(MonitorViewModel monitor)
        {
            _monitor = monitor;
            _monitor.ReloadScopeSettings();
        }

        /// <summary>
        /// Tells the MonitorViewModel to re-read every scope_* AppConfig
        /// key and update its observable display properties. Called from
        /// ScopeSettingsViewModel whenever any setting changes.
        /// </summary>
        public static void ApplySettings()
        {
            _monitor?.ReloadScopeSettings();
        }
    }
}
```

- [ ] **Step 2: Wire Configure into MainWindow startup**

In `app/GHelper.WPF/Views/MainWindow.xaml.cs`, find the existing `GadgetService.Configure` call (now `Configure(mainVm.Monitor, mainVm.ModeStrip)` from Task 4). **Add** a line right after it:

```csharp
GadgetService.Configure(mainVm.Monitor, mainVm.ModeStrip);
ScopeService.Configure(mainVm.Monitor);
```

**Note:** Don't build or commit yet — Task 9 lands `MonitorViewModel.ReloadScopeSettings`, which this file calls.

---

## Task 9: Extend MonitorViewModel for history window, accent, size

**Files:**
- Modify: `app/GHelper.WPF/ViewModels/MonitorViewModel.cs`

- [ ] **Step 1: Grow buffers and add tile-visibility/style/history properties**

In `MonitorViewModel.cs`, replace the `HistorySize` constant and add new state. The header of the class becomes:

```csharp
public partial class MonitorViewModel : ObservableObject
{
    private static readonly System.Windows.Media.SolidColorBrush GreenBrush;
    private static readonly System.Windows.Media.SolidColorBrush AmberBrush;
    private static readonly System.Windows.Media.SolidColorBrush OrangeBrush;
    private static readonly System.Windows.Media.SolidColorBrush RedBrush;

    static MonitorViewModel() { /* existing brush init unchanged */ }

    // Buffers now hold the longest possible window (15 min @ 1s tick = 900).
    // Shorter windows are rendered by slicing the tail.
    private const int BufferSize = 900;

    private readonly double[] _cpuTempHistory = new double[BufferSize];
    private readonly double[] _gpuTempHistory = new double[BufferSize];
    private readonly double[] _cpuFanHistory = new double[BufferSize];
    private readonly double[] _gpuFanHistory = new double[BufferSize];
    private readonly double[] _gpuUseHistory = new double[BufferSize];
    private readonly double[] _gpuPowerHistory = new double[BufferSize];
    private readonly double[] _cpuUseHistory = new double[BufferSize];
    private readonly double[] _batteryRateHistory = new double[BufferSize];

    // ---- Scope settings (driven by ScopeService.ApplySettings) ----
    [ObservableProperty] private bool _scopeShowCpuTemp = true;
    [ObservableProperty] private bool _scopeShowDgpuTemp = true;
    [ObservableProperty] private bool _scopeShowCpuUse = true;
    [ObservableProperty] private bool _scopeShowDgpuUse = true;
    [ObservableProperty] private bool _scopeShowPower = true;
    [ObservableProperty] private bool _scopeShowBattery = true;
    [ObservableProperty] private bool _scopeShowCpuFan = true;
    [ObservableProperty] private bool _scopeShowGpuFan = true;

    // 80 / 106 / 130 — derived from "compact" / "regular" / "large".
    [ObservableProperty] private double _sparklineHeight = 106;
    // 22 / 28 / 32 — value text size matching the size preset.
    [ObservableProperty] private double _valueFontSize = 28;
    // Smaller value font for fan tiles (RPM digits) — keep proportional.
    [ObservableProperty] private double _valueFontSizeFan = 22;

    // 60 / 300 / 900 — number of trailing samples to render.
    [ObservableProperty] private int _historyWindowSeconds = 60;

    // Per-tile stroke colors. On "multi" these match the existing hardcoded
    // sparkline colors. On a single accent, all eight tint to that color.
    [ObservableProperty] private System.Windows.Media.Color _cpuTempStroke =
        System.Windows.Media.Color.FromRgb(0xFF, 0x6B, 0x6B);
    [ObservableProperty] private System.Windows.Media.Color _gpuTempStroke =
        System.Windows.Media.Color.FromRgb(0xFF, 0xB3, 0x47);
    [ObservableProperty] private System.Windows.Media.Color _cpuUseStroke =
        System.Windows.Media.Color.FromRgb(0xA7, 0x8B, 0xFA);
    [ObservableProperty] private System.Windows.Media.Color _gpuUseStroke =
        System.Windows.Media.Color.FromRgb(0x60, 0xCD, 0xFF);
    [ObservableProperty] private System.Windows.Media.Color _gpuPowerStroke =
        System.Windows.Media.Color.FromRgb(0x6B, 0xCB, 0x77);
    [ObservableProperty] private System.Windows.Media.Color _cpuFanStroke =
        System.Windows.Media.Color.FromRgb(0xC0, 0x84, 0xFC);
    [ObservableProperty] private System.Windows.Media.Color _gpuFanStroke =
        System.Windows.Media.Color.FromRgb(0xF4, 0x72, 0xB6);

    // ... rest of the existing properties (CpuTempValues, BatteryBrush, etc.) unchanged ...
}
```

- [ ] **Step 2: Update the value-publishing path so it returns the visible tail**

Right now `UpdateSensors` does `CpuTempValues = (double[])_cpuTempHistory.Clone();`, copying the whole 60-element buffer. With a 900-element buffer that would always render 900 points. Add a helper that takes the last N samples:

Add this method to `MonitorViewModel`:

```csharp
private double[] TailOf(double[] buffer)
{
    int n = Math.Clamp(HistoryWindowSeconds, 1, buffer.Length);
    var result = new double[n];
    Array.Copy(buffer, buffer.Length - n, result, 0, n);
    return result;
}
```

In `UpdateSensors()`, replace each `(double[])_xxxHistory.Clone()` with `TailOf(_xxxHistory)`:

```csharp
// Before
CpuTempValues = (double[])_cpuTempHistory.Clone();
// After
CpuTempValues = TailOf(_cpuTempHistory);
```

Apply this to all eight `*Values` assignments in `UpdateSensors`.

- [ ] **Step 3: Add ReloadScopeSettings**

Append this method to the class:

```csharp
/// <summary>
/// Re-reads every scope_* AppConfig key and updates the corresponding
/// observable properties. Called by ScopeService.ApplySettings on any
/// settings change. Bindings in MonitorPanel.xaml react automatically.
/// </summary>
public void ReloadScopeSettings()
{
    ScopeShowCpuTemp = AppConfig.Get("scope_show_cpu_temp", 1) == 1;
    ScopeShowDgpuTemp = AppConfig.Get("scope_show_dgpu_temp", 1) == 1;
    ScopeShowCpuUse = AppConfig.Get("scope_show_cpu_use", 1) == 1;
    ScopeShowDgpuUse = AppConfig.Get("scope_show_dgpu_use", 1) == 1;
    ScopeShowPower = AppConfig.Get("scope_show_power", 1) == 1;
    ScopeShowBattery = AppConfig.Get("scope_show_battery", 1) == 1;
    ScopeShowCpuFan = AppConfig.Get("scope_show_cpu_fan", 1) == 1;
    ScopeShowGpuFan = AppConfig.Get("scope_show_gpu_fan", 1) == 1;

    // Size preset → sparkline height + value font size.
    string size = AppConfig.GetString("scope_size") ?? "regular";
    (SparklineHeight, ValueFontSize, ValueFontSizeFan) = size switch
    {
        "compact" => (80.0, 22.0, 18.0),
        "large"   => (130.0, 32.0, 26.0),
        _         => (106.0, 28.0, 22.0),
    };

    // History window → number of samples to render.
    string window = AppConfig.GetString("scope_history_window") ?? "60s";
    HistoryWindowSeconds = window switch
    {
        "5min"  => 300,
        "15min" => 900,
        _       => 60,
    };

    // Accent → per-tile stroke colors.
    string accent = AppConfig.GetString("scope_accent") ?? "multi";
    if (accent == "multi")
    {
        // Default per-tile palette.
        CpuTempStroke = System.Windows.Media.Color.FromRgb(0xFF, 0x6B, 0x6B);
        GpuTempStroke = System.Windows.Media.Color.FromRgb(0xFF, 0xB3, 0x47);
        CpuUseStroke = System.Windows.Media.Color.FromRgb(0xA7, 0x8B, 0xFA);
        GpuUseStroke = System.Windows.Media.Color.FromRgb(0x60, 0xCD, 0xFF);
        GpuPowerStroke = System.Windows.Media.Color.FromRgb(0x6B, 0xCB, 0x77);
        CpuFanStroke = System.Windows.Media.Color.FromRgb(0xC0, 0x84, 0xFC);
        GpuFanStroke = System.Windows.Media.Color.FromRgb(0xF4, 0x72, 0xB6);
    }
    else
    {
        var c = AccentColor(accent);
        CpuTempStroke = c;
        GpuTempStroke = c;
        CpuUseStroke = c;
        GpuUseStroke = c;
        GpuPowerStroke = c;
        CpuFanStroke = c;
        GpuFanStroke = c;
    }

    // Republish the *Values arrays so the new HistoryWindowSeconds takes
    // effect on the next render — without waiting for the next sensor tick.
    CpuTempValues = TailOf(_cpuTempHistory);
    GpuTempValues = TailOf(_gpuTempHistory);
    CpuUseValues = TailOf(_cpuUseHistory);
    GpuUseValues = TailOf(_gpuUseHistory);
    GpuPowerValues = TailOf(_gpuPowerHistory);
    CpuFanValues = TailOf(_cpuFanHistory);
    GpuFanValues = TailOf(_gpuFanHistory);
    BatteryRateValues = TailOf(_batteryRateHistory);
}

private static System.Windows.Media.Color AccentColor(string key) => key switch
{
    "blue"   => System.Windows.Media.Color.FromRgb(0x60, 0xCD, 0xFF),
    "purple" => System.Windows.Media.Color.FromRgb(0xC0, 0x84, 0xFC),
    "green"  => System.Windows.Media.Color.FromRgb(0x6B, 0xCB, 0x77),
    "orange" => System.Windows.Media.Color.FromRgb(0xFF, 0xB3, 0x47),
    "red"    => System.Windows.Media.Color.FromRgb(0xFF, 0x6B, 0x6B),
    "white"  => System.Windows.Media.Color.FromRgb(0xF0, 0xF0, 0xF2),
    "dark"   => System.Windows.Media.Color.FromRgb(0x36, 0x36, 0x3B),
    _        => System.Windows.Media.Color.FromRgb(0x60, 0xCD, 0xFF),
};
```

- [ ] **Step 4: Build**

```bash
dotnet build app/GHelper.WPF/GHelper.WPF.csproj
```

Expected: 0 errors.

- [ ] **Step 5: Smoke test**

```bash
taskkill //F //IM "G-Aether.exe" 2>/dev/null || true
dotnet run --project app/GHelper.WPF/GHelper.WPF.csproj
```

Visual checks:
- Monitor panel still renders correctly (the bindings haven't changed yet, so it should look exactly the same as before).
- No exceptions in `%APPDATA%\GHelper\log.txt`.
- The sparklines render the most recent ~60 seconds of history (the existing visual) — the buffer is bigger now but the visible tail is still 60s by default.

- [ ] **Step 6: Commit (bundles Tasks 7 + 8 + 9)**

```bash
git add app/GHelper.WPF/ViewModels/ScopeSettingsViewModel.cs \
        app/GHelper.WPF/Services/ScopeService.cs \
        app/GHelper.WPF/Views/MainWindow.xaml.cs \
        app/GHelper.WPF/ViewModels/MonitorViewModel.cs
git commit -m "feat: add ScopeService + ScopeSettingsViewModel; extend MonitorViewModel"
```

---

## Task 10: Bind MonitorPanel sparklines to scope properties

**Files:**
- Modify: `app/GHelper.WPF/Views/Panels/MonitorPanel.xaml`

- [ ] **Step 1: Bind size, color, and visibility on every SparklineChart**

For each of the seven `SparklineChart` controls in `MonitorPanel.xaml`, change:

- `Height="106"` → `Height="{Binding SparklineHeight}"`
- `ValueFontSize="28"` (temperature/usage/power tiles) → `ValueFontSize="{Binding ValueFontSize}"`
- `ValueFontSize="22"` (fan tiles) → `ValueFontSize="{Binding ValueFontSizeFan}"`
- `StrokeColor="#FF6B6B"` (CPU Temp) → `StrokeColor="{Binding CpuTempStroke}"`
- `StrokeColor="#FFB347"` (dGPU Temp) → `StrokeColor="{Binding GpuTempStroke}"`
- `StrokeColor="#A78BFA"` (CPU Usage) → `StrokeColor="{Binding CpuUseStroke}"`
- `StrokeColor="#60CDFF"` (dGPU Usage) → `StrokeColor="{Binding GpuUseStroke}"`
- `StrokeColor="#6BCB77"` (dGPU Power) → `StrokeColor="{Binding GpuPowerStroke}"`
- `StrokeColor="#C084FC"` (CPU Fan) → `StrokeColor="{Binding CpuFanStroke}"`
- `StrokeColor="#F472B6"` (GPU Fan) → `StrokeColor="{Binding GpuFanStroke}"`

If `SparklineChart`'s `StrokeColor` property is typed `Color` (not `SolidColorBrush`), the binding will work directly to the Color-typed properties on the VM. If it's typed `Brush`, add a `ColorToBrushConverter` somewhere (or change the VM properties to `SolidColorBrush`). **Check the type before this step:**

```bash
grep -n "StrokeColor" app/GHelper.WPF/Controls/*.cs
```

If it's `Color`, no converter needed. If it's `Brush`, change the eight `[ObservableProperty] private Color _xxxStroke` declarations in Task 9 to `SolidColorBrush` and update the `ReloadScopeSettings` body to `new SolidColorBrush(...)` (and freeze each one).

- [ ] **Step 2: Add per-tile visibility bindings**

Each `SparklineChart` (and the dGPU-off banner, and the battery row) gets a `Visibility` binding. Use the existing `BoolToVisibility` converter (referenced in the file already).

Examples:

```xml
<!-- CPU Temp -->
<controls:SparklineChart Grid.Row="2" Grid.Column="0"
    Height="{Binding SparklineHeight}"
    Label="CPU Temp"
    CurrentText="{Binding CpuTempText}"
    Values="{Binding CpuTempValues}"
    MaxValue="105"
    ValueFontSize="{Binding ValueFontSize}"
    StrokeColor="{Binding CpuTempStroke}"
    Visibility="{Binding ScopeShowCpuTemp, Converter={StaticResource BoolToVisibility}}" />

<!-- CPU Usage -->
<controls:SparklineChart Grid.Row="2" Grid.Column="2"
    Height="{Binding SparklineHeight}"
    Label="CPU Usage"
    CurrentText="{Binding CpuUseText}"
    Values="{Binding CpuUseValues}"
    MaxValue="100"
    ValueFontSize="{Binding ValueFontSize}"
    StrokeColor="{Binding CpuUseStroke}"
    Visibility="{Binding ScopeShowCpuUse, Converter={StaticResource BoolToVisibility}}" />
```

For the dGPU temp + usage tiles, the `Visibility` is *already* governed by `IsGpuActive`. We need both conditions: tile is shown only if `ScopeShowDgpuTemp` is true AND `IsGpuActive` is true. The simplest fix: introduce two derived properties on `MonitorViewModel`:

Add to `MonitorViewModel.cs` (anywhere among the other observables):

```csharp
public bool ShouldShowDgpuTemp => ScopeShowDgpuTemp && IsGpuActive;
public bool ShouldShowDgpuUse => ScopeShowDgpuUse && IsGpuActive;
```

And add these `NotifyPropertyChangedFor` to existing observable declarations:

```csharp
[ObservableProperty]
[NotifyPropertyChangedFor(nameof(ShouldShowDgpuTemp))]
private bool _scopeShowDgpuTemp = true;

[ObservableProperty]
[NotifyPropertyChangedFor(nameof(ShouldShowDgpuUse))]
private bool _scopeShowDgpuUse = true;

[ObservableProperty]
[NotifyPropertyChangedFor(nameof(ShouldShowDgpuTemp), nameof(ShouldShowDgpuUse))]
private bool _isGpuActive = true;
```

In `MonitorPanel.xaml`, the dGPU sparklines bind to `ShouldShowDgpuTemp` / `ShouldShowDgpuUse` instead of `IsGpuActive`:

```xml
Visibility="{Binding ShouldShowDgpuTemp, Converter={StaticResource BoolToVisibility}}"
```

The "dGPU Off" banner stays bound to the existing `IsGpuActive` (with the inverse converter) — but should *also* be hidden if both dGPU toggles are off. Drop the banner binding and use the simpler:

```xml
Visibility="{Binding IsGpuActive, Converter={StaticResource InverseBoolToVisibility}}"
```

(Banner appears when GPU is parked, regardless of toggles. That's the desired behavior — the banner is contextual UX, not a tile.)

For the **battery row** (the special bottom row with charge percent + halo), wrap the existing `Grid` in a container with:

```xml
Visibility="{Binding ScopeShowBattery, Converter={StaticResource BoolToVisibility}}"
```

For the fan sparklines:

```xml
<!-- CPU Fan -->
<controls:SparklineChart Grid.Row="8" Grid.Column="0"
    Height="{Binding SparklineHeight}"
    Label="CPU Fan"
    CurrentText="{Binding CpuFanCurrentText}"
    Values="{Binding CpuFanValues}"
    MaxValue="7000"
    ValueFontSize="{Binding ValueFontSizeFan}"
    StrokeColor="{Binding CpuFanStroke}"
    Visibility="{Binding ScopeShowCpuFan, Converter={StaticResource BoolToVisibility}}" />
```

…and similarly for GPU Fan and dGPU Power.

- [ ] **Step 3: Build**

```bash
dotnet build app/GHelper.WPF/GHelper.WPF.csproj
```

Expected: 0 errors.

- [ ] **Step 4: Smoke test**

```bash
taskkill //F //IM "G-Aether.exe" 2>/dev/null || true
dotnet run --project app/GHelper.WPF/GHelper.WPF.csproj
```

Visual checks (open Monitor panel):
- All sparklines render at the expected size (compact would shrink them — but we haven't built the settings UI yet, so they should appear at "regular" 106px height with default colors).
- Edit `%APPDATA%\GHelper\config.json` manually: set `"scope_show_cpu_temp": 0`, restart the app — CPU Temp tile is hidden.
- Set `"scope_size": "compact"` → tiles shrink. Set to `"large"` → tiles grow.
- Set `"scope_accent": "blue"` → all sparklines turn blue. Set back to `"multi"` (or remove the key) → original colors restored.
- Set `"scope_history_window": "5min"` → sparklines render more sample points (time-resolution-wise, but only after enough samples have accumulated post-launch).

(Manual config editing is OK as a smoke test until Task 13 ships the UI. Reset `config.json` afterward.)

- [ ] **Step 5: Commit**

```bash
git add app/GHelper.WPF/Views/Panels/MonitorPanel.xaml \
        app/GHelper.WPF/ViewModels/MonitorViewModel.cs
git commit -m "feat: bind MonitorPanel sparklines to scope settings"
```

---

## Task 11: Create ScopeSettingsWindow shell

**Files:**
- Create: `app/GHelper.WPF/Views/ScopeSettingsWindow.xaml`
- Create: `app/GHelper.WPF/Views/ScopeSettingsWindow.xaml.cs`

- [ ] **Step 1: Create the XAML — same chrome as GadgetSettingsWindow but empty content**

```xml
<!-- app/GHelper.WPF/Views/ScopeSettingsWindow.xaml -->
<Window x:Class="GHelper.WPF.Views.ScopeSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:GHelper.WPF.Controls"
        Title="G-Scope Settings"
        Width="500" Height="620"
        MinWidth="480" MinHeight="400"
        MaxHeight="900"
        ResizeMode="CanResizeWithGrip"
        WindowStyle="None"
        AllowsTransparency="True"
        WindowStartupLocation="CenterOwner"
        Background="Transparent"
        Foreground="{DynamicResource ForeMainBrush}"
        FontFamily="Segoe UI Variable"
        ShowInTaskbar="False">

    <Border Background="{DynamicResource WindowBackgroundBrush}" CornerRadius="12"
            BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Title bar -->
            <Grid Grid.Row="0" Height="34" Background="Transparent"
                  MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                    <TextBlock Text="&#xE9D9;" FontFamily="Segoe MDL2 Assets"
                               FontSize="13" Foreground="{DynamicResource AccentBrush}"
                               VerticalAlignment="Center" Margin="0,0,8,0" />
                    <TextBlock Text="G-Scope Settings" FontSize="12" FontWeight="SemiBold"
                               Foreground="{DynamicResource ForeMainBrush}"
                               VerticalAlignment="Center" />
                </StackPanel>
                <Button x:Name="CloseButton"
                        Content="&#xE8BB;"
                        HorizontalAlignment="Right" VerticalAlignment="Center"
                        Width="32" Height="28"
                        Click="CloseButton_Click"
                        Style="{StaticResource CloseButtonStyle}" />
            </Grid>

            <Rectangle Grid.Row="1" Height="1" Fill="#2060CDFF" Margin="14,0" />

            <!-- Content (filled in Task 12) -->
            <ScrollViewer Grid.Row="2"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Padding="20,14,20,8">
                <StackPanel x:Name="ContentStack" />
            </ScrollViewer>

            <!-- Footer -->
            <Border Grid.Row="3" BorderBrush="#18FFFFFF" BorderThickness="0,1,0,0" Padding="20,10,20,14">
                <Button Content="Done"
                        HorizontalAlignment="Right"
                        Click="CloseButton_Click"
                        Background="#20FFFFFF" Foreground="White"
                        BorderThickness="0" Padding="20,5"
                        FontSize="11" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4" />
                        </Style>
                    </Button.Resources>
                </Button>
            </Border>
        </Grid>
    </Border>
</Window>
```

- [ ] **Step 2: Create the code-behind — match GadgetSettingsWindow's pattern**

```csharp
// app/GHelper.WPF/Views/ScopeSettingsWindow.xaml.cs
using System.Windows;
using System.Windows.Input;
using GHelper.WPF.ViewModels;

namespace GHelper.WPF.Views
{
    /// <summary>
    /// Modal page for configuring the G-Scope panel. Mirrors GadgetSettingsWindow
    /// but uses its own ScopeSettingsViewModel instance — gadget and panel
    /// settings are independent by design.
    /// </summary>
    public partial class ScopeSettingsWindow : Window
    {
        public ScopeSettingsWindow()
        {
            InitializeComponent();
        }

        public static void Show(Window? owner)
        {
            var win = new ScopeSettingsWindow { DataContext = new ScopeSettingsViewModel() };
            if (owner != null) win.Owner = owner;
            win.ShowDialog();
        }

        private void TitleBar_MouseLeftButtonDown(object sender, MouseButtonEventArgs e)
        {
            if (e.ClickCount == 1) DragMove();
        }

        private void CloseButton_Click(object sender, RoutedEventArgs e) => Close();
    }
}
```

- [ ] **Step 3: Build**

```bash
dotnet build app/GHelper.WPF/GHelper.WPF.csproj
```

Expected: 0 errors. The window is functional but empty — content is filled in Task 12.

- [ ] **Step 4: Commit**

```bash
git add app/GHelper.WPF/Views/ScopeSettingsWindow.xaml \
        app/GHelper.WPF/Views/ScopeSettingsWindow.xaml.cs
git commit -m "feat: add empty ScopeSettingsWindow shell"
```

---

## Task 12: Build out ScopeSettingsWindow content

**Files:**
- Modify: `app/GHelper.WPF/Views/ScopeSettingsWindow.xaml`

- [ ] **Step 1: Replace the empty `<StackPanel x:Name="ContentStack" />` with the four cards**

```xml
<StackPanel>

    <!-- ===================================================== -->
    <!-- VISIBLE TILES                                         -->
    <!-- ===================================================== -->
    <Border Style="{StaticResource CardStyle}">
        <StackPanel>
            <TextBlock Text="Visible Tiles" Style="{StaticResource CardTitleStyle}" Margin="0,0,0,4" />
            <TextBlock Text="Pick which sparklines show on the G-Scope panel."
                       Style="{StaticResource DimTextStyle}" FontSize="11" Margin="0,0,0,12" TextWrapping="Wrap" />

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Grid Grid.Row="0" Grid.Column="0" Margin="0,0,12,12">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <TextBlock Text="CPU Temp" Style="{StaticResource BodyTextStyle}" VerticalAlignment="Center" />
                    <controls:ToggleSwitch Grid.Column="1" IsChecked="{Binding ShowCpuTemp, Mode=TwoWay}" Width="44" Height="22" />
                </Grid>
                <Grid Grid.Row="0" Grid.Column="1" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <TextBlock Text="dGPU Temp" Style="{StaticResource BodyTextStyle}" VerticalAlignment="Center" />
                    <controls:ToggleSwitch Grid.Column="1" IsChecked="{Binding ShowDgpuTemp, Mode=TwoWay}" Width="44" Height="22" />
                </Grid>
                <Grid Grid.Row="1" Grid.Column="0" Margin="0,0,12,12">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <TextBlock Text="CPU Use" Style="{StaticResource BodyTextStyle}" VerticalAlignment="Center" />
                    <controls:ToggleSwitch Grid.Column="1" IsChecked="{Binding ShowCpuUse, Mode=TwoWay}" Width="44" Height="22" />
                </Grid>
                <Grid Grid.Row="1" Grid.Column="1" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <TextBlock Text="dGPU Use" Style="{StaticResource BodyTextStyle}" VerticalAlignment="Center" />
                    <controls:ToggleSwitch Grid.Column="1" IsChecked="{Binding ShowDgpuUse, Mode=TwoWay}" Width="44" Height="22" />
                </Grid>
                <Grid Grid.Row="2" Grid.Column="0" Margin="0,0,12,12">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <TextBlock Text="dGPU Power" Style="{StaticResource BodyTextStyle}" VerticalAlignment="Center" />
                    <controls:ToggleSwitch Grid.Column="1" IsChecked="{Binding ShowPower, Mode=TwoWay}" Width="44" Height="22" />
                </Grid>
                <Grid Grid.Row="2" Grid.Column="1" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <TextBlock Text="Battery" Style="{StaticResource BodyTextStyle}" VerticalAlignment="Center" />
                    <controls:ToggleSwitch Grid.Column="1" IsChecked="{Binding ShowBattery, Mode=TwoWay}" Width="44" Height="22" />
                </Grid>
                <Grid Grid.Row="3" Grid.Column="0" Margin="0,0,12,0">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <TextBlock Text="CPU Fan" Style="{StaticResource BodyTextStyle}" VerticalAlignment="Center" />
                    <controls:ToggleSwitch Grid.Column="1" IsChecked="{Binding ShowCpuFan, Mode=TwoWay}" Width="44" Height="22" />
                </Grid>
                <Grid Grid.Row="3" Grid.Column="1">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <TextBlock Text="GPU Fan" Style="{StaticResource BodyTextStyle}" VerticalAlignment="Center" />
                    <controls:ToggleSwitch Grid.Column="1" IsChecked="{Binding ShowGpuFan, Mode=TwoWay}" Width="44" Height="22" />
                </Grid>
            </Grid>
        </StackPanel>
    </Border>

    <!-- ===================================================== -->
    <!-- APPEARANCE                                            -->
    <!-- ===================================================== -->
    <Border Style="{StaticResource CardStyle}">
        <StackPanel>
            <TextBlock Text="Appearance" Style="{StaticResource CardTitleStyle}" Margin="0,0,0,12" />

            <!-- Size -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Size" Style="{StaticResource BodyTextStyle}" />
                    <TextBlock Text="Sparkline density on the G-Scope panel"
                               Style="{StaticResource DimTextStyle}" FontSize="10" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="Compact" CommandParameter="compact" Command="{Binding SetScopeSizeCommand}" Margin="0,0,4,0">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource SecondaryActionButtonStyle}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsScopeSizeCompact}" Value="True">
                                        <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                                        <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button Content="Regular" CommandParameter="regular" Command="{Binding SetScopeSizeCommand}" Margin="0,0,4,0">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource SecondaryActionButtonStyle}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsScopeSizeRegular}" Value="True">
                                        <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                                        <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button Content="Large" CommandParameter="large" Command="{Binding SetScopeSizeCommand}">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource SecondaryActionButtonStyle}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsScopeSizeLarge}" Value="True">
                                        <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                                        <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- Accent — same eight swatches as the gadget; selected swatch gets a white ring. -->
            <Grid Margin="0,14,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Accent color" Style="{StaticResource BodyTextStyle}" />
                    <TextBlock Text="Multi keeps each tile's original color"
                               Style="{StaticResource DimTextStyle}" FontSize="10" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <!-- Multi -->
                    <Button CommandParameter="multi" Command="{Binding SetScopeAccentCommand}"
                            Width="22" Height="22" Padding="0" Margin="0,0,6,0" Cursor="Hand" ToolTip="Multi (default)">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Grid>
                                                <Ellipse>
                                                    <Ellipse.Fill>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                            <GradientStop Color="#FF6B6B" Offset="0.0" />
                                                            <GradientStop Color="#FFB347" Offset="0.2" />
                                                            <GradientStop Color="#6BCB77" Offset="0.4" />
                                                            <GradientStop Color="#60CDFF" Offset="0.6" />
                                                            <GradientStop Color="#A78BFA" Offset="0.8" />
                                                            <GradientStop Color="#F472B6" Offset="1.0" />
                                                        </LinearGradientBrush>
                                                    </Ellipse.Fill>
                                                </Ellipse>
                                                <Ellipse x:Name="Ring" Stroke="White" StrokeThickness="2" Visibility="Collapsed" />
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <DataTrigger Binding="{Binding IsAccentMulti}" Value="True">
                                                    <Setter TargetName="Ring" Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                    <!-- Blue -->
                    <Button CommandParameter="blue" Command="{Binding SetScopeAccentCommand}"
                            Width="22" Height="22" Padding="0" Margin="0,0,6,0" Cursor="Hand" ToolTip="Blue">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Grid>
                                                <Ellipse Fill="#60CDFF" />
                                                <Ellipse x:Name="Ring" Stroke="White" StrokeThickness="2" Visibility="Collapsed" />
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <DataTrigger Binding="{Binding IsAccentBlue}" Value="True">
                                                    <Setter TargetName="Ring" Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                    <!-- Purple -->
                    <Button CommandParameter="purple" Command="{Binding SetScopeAccentCommand}"
                            Width="22" Height="22" Padding="0" Margin="0,0,6,0" Cursor="Hand" ToolTip="Purple">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Grid>
                                                <Ellipse Fill="#C084FC" />
                                                <Ellipse x:Name="Ring" Stroke="White" StrokeThickness="2" Visibility="Collapsed" />
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <DataTrigger Binding="{Binding IsAccentPurple}" Value="True">
                                                    <Setter TargetName="Ring" Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                    <!-- Green -->
                    <Button CommandParameter="green" Command="{Binding SetScopeAccentCommand}"
                            Width="22" Height="22" Padding="0" Margin="0,0,6,0" Cursor="Hand" ToolTip="Green">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Grid>
                                                <Ellipse Fill="#6BCB77" />
                                                <Ellipse x:Name="Ring" Stroke="White" StrokeThickness="2" Visibility="Collapsed" />
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <DataTrigger Binding="{Binding IsAccentGreen}" Value="True">
                                                    <Setter TargetName="Ring" Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                    <!-- Orange -->
                    <Button CommandParameter="orange" Command="{Binding SetScopeAccentCommand}"
                            Width="22" Height="22" Padding="0" Margin="0,0,6,0" Cursor="Hand" ToolTip="Orange">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Grid>
                                                <Ellipse Fill="#FFB347" />
                                                <Ellipse x:Name="Ring" Stroke="White" StrokeThickness="2" Visibility="Collapsed" />
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <DataTrigger Binding="{Binding IsAccentOrange}" Value="True">
                                                    <Setter TargetName="Ring" Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                    <!-- Red -->
                    <Button CommandParameter="red" Command="{Binding SetScopeAccentCommand}"
                            Width="22" Height="22" Padding="0" Margin="0,0,6,0" Cursor="Hand" ToolTip="Red">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Grid>
                                                <Ellipse Fill="#FF6B6B" />
                                                <Ellipse x:Name="Ring" Stroke="White" StrokeThickness="2" Visibility="Collapsed" />
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <DataTrigger Binding="{Binding IsAccentRed}" Value="True">
                                                    <Setter TargetName="Ring" Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                    <!-- White -->
                    <Button CommandParameter="white" Command="{Binding SetScopeAccentCommand}"
                            Width="22" Height="22" Padding="0" Margin="0,0,6,0" Cursor="Hand" ToolTip="White">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Grid>
                                                <Ellipse Fill="#F0F0F2" Stroke="#22FFFFFF" StrokeThickness="1" />
                                                <Ellipse x:Name="Ring" Stroke="#60CDFF" StrokeThickness="2" Visibility="Collapsed" />
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <DataTrigger Binding="{Binding IsAccentWhite}" Value="True">
                                                    <Setter TargetName="Ring" Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                    <!-- Dark / Stealth -->
                    <Button CommandParameter="dark" Command="{Binding SetScopeAccentCommand}"
                            Width="22" Height="22" Padding="0" Cursor="Hand" ToolTip="Stealth (near-black)">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Grid>
                                                <Ellipse Fill="#36363B" Stroke="#22FFFFFF" StrokeThickness="1" />
                                                <Ellipse x:Name="Ring" Stroke="White" StrokeThickness="2" Visibility="Collapsed" />
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <DataTrigger Binding="{Binding IsAccentDark}" Value="True">
                                                    <Setter TargetName="Ring" Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Border>

    <!-- ===================================================== -->
    <!-- HISTORY                                               -->
    <!-- ===================================================== -->
    <Border Style="{StaticResource CardStyle}">
        <StackPanel>
            <TextBlock Text="History" Style="{StaticResource CardTitleStyle}" Margin="0,0,0,4" />
            <TextBlock Text="How far back the sparklines extend. Longer windows are useful for spotting slow trends; the gadget keeps its own short window."
                       Style="{StaticResource DimTextStyle}" FontSize="11" Margin="0,0,0,12" TextWrapping="Wrap" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Window" Style="{StaticResource BodyTextStyle}" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="60s" CommandParameter="60s" Command="{Binding SetScopeHistoryCommand}" Margin="0,0,4,0">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource SecondaryActionButtonStyle}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsHistory60s}" Value="True">
                                        <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                                        <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button Content="5 min" CommandParameter="5min" Command="{Binding SetScopeHistoryCommand}" Margin="0,0,4,0">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource SecondaryActionButtonStyle}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsHistory5min}" Value="True">
                                        <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                                        <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button Content="15 min" CommandParameter="15min" Command="{Binding SetScopeHistoryCommand}">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource SecondaryActionButtonStyle}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsHistory15min}" Value="True">
                                        <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                                        <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Border>

</StackPanel>
```

Note the comment in the Accent section: copy the seven non-multi swatch buttons from `GadgetSettingsWindow.xaml` (lines 315–471 in the existing file) and substitute the command name. The `IsAccent*` property names are identical between `ExtraSettingsViewModel` and `ScopeSettingsViewModel`, so the `DataTrigger` bindings work without modification.

- [ ] **Step 2: Build**

```bash
dotnet build app/GHelper.WPF/GHelper.WPF.csproj
```

Expected: 0 errors.

- [ ] **Step 3: Commit**

```bash
git add app/GHelper.WPF/Views/ScopeSettingsWindow.xaml
git commit -m "feat: build out G-Scope settings content"
```

(Visual smoke test for this window happens in Task 13 once it's reachable.)

---

## Task 13: Add the gear button to MonitorPanel

**Files:**
- Modify: `app/GHelper.WPF/Views/Panels/MonitorPanel.xaml`
- Modify: `app/GHelper.WPF/Views/Panels/MonitorPanel.xaml.cs`

- [ ] **Step 1: Add the gear button next to the help button in the header**

In `MonitorPanel.xaml`, the header `StackPanel` contains the icon, "G-Scope" title, and `<controls:HelpButton />`. **Add** a gear button after the help button:

```xml
<StackPanel Grid.Row="0" Grid.ColumnSpan="3"
            Orientation="Horizontal">
    <TextBlock Text="&#xE9D9;" FontFamily="Segoe MDL2 Assets" FontSize="16"
               Foreground="{DynamicResource AccentBrush}"
               VerticalAlignment="Center" Margin="0,0,8,0" />
    <TextBlock Text="G-Scope"
               Style="{StaticResource CardTitleStyle}"
               VerticalAlignment="Center" />
    <controls:HelpButton HelpKey="monitor_telemetry" VerticalAlignment="Center" Margin="8,0,0,0" />
    <Button x:Name="ScopeSettingsButton"
            Click="ScopeSettingsButton_Click"
            VerticalAlignment="Center"
            Margin="6,0,0,0"
            Width="22" Height="22"
            Padding="0"
            Background="Transparent"
            BorderThickness="0"
            Cursor="Hand"
            ToolTip="G-Scope settings">
        <TextBlock Text="&#xE713;"
                   FontFamily="Segoe MDL2 Assets"
                   FontSize="13"
                   Foreground="{DynamicResource ForeDimBrush}" />
    </Button>
</StackPanel>
```

- [ ] **Step 2: Add the click handler in code-behind**

In `app/GHelper.WPF/Views/Panels/MonitorPanel.xaml.cs`, add:

```csharp
using System.Windows;
using GHelper.WPF.Views;

// Inside the partial class:
private void ScopeSettingsButton_Click(object sender, RoutedEventArgs e)
{
    ScopeSettingsWindow.Show(Window.GetWindow(this));
}
```

If `MonitorPanel.xaml.cs` is empty (only `InitializeComponent()`), add the using and the method to the existing class declaration. If imports already exist, add only the `using GHelper.WPF.Views;` line if missing.

- [ ] **Step 3: Build**

```bash
dotnet build app/GHelper.WPF/GHelper.WPF.csproj
```

Expected: 0 errors.

- [ ] **Step 4: Smoke test (full integration)**

```bash
taskkill //F //IM "G-Aether.exe" 2>/dev/null || true
dotnet run --project app/GHelper.WPF/GHelper.WPF.csproj
```

Visual checks (open the G-Scope panel):
- Header reads "G-Scope" with help (?) and gear (⚙) buttons next to it.
- Click the gear button → "G-Scope Settings" window opens, modal, centered on the main window.
- Toggle "CPU Temp" off → CPU Temp tile vanishes immediately (no apply button needed).
- Click size "Compact" → tiles shrink in real time. "Large" → grow. "Regular" → original size.
- Click accent "Blue" → all sparklines tint blue. Click "Multi" → restore per-tile colors.
- Click history "5 min" → sparklines render more samples.
- Close the window. Reopen it — every setting persisted.
- The floating gadget's tile visibility is **independent** — turning off panel tiles does not hide gadget tiles, and vice versa. Verify this by changing one and checking the other unchanged.
- Open `%APPDATA%\GHelper\config.json` — confirm `scope_show_*`, `scope_accent`, `scope_size`, `scope_history_window` keys are all present and reflect the latest selections.

- [ ] **Step 5: Commit**

```bash
git add app/GHelper.WPF/Views/Panels/MonitorPanel.xaml \
        app/GHelper.WPF/Views/Panels/MonitorPanel.xaml.cs
git commit -m "feat: open G-Scope settings from gear button in panel header"
```

---

## Task 14: Final integration smoke test

- [ ] **Step 1: Full end-to-end visual check**

```bash
taskkill //F //IM "G-Aether.exe" 2>/dev/null || true
dotnet run --project app/GHelper.WPF/GHelper.WPF.csproj
```

Walk through every behavior from the spec:

**Floating gadget:**
- Mode strip appears with all four badges in correct order (Perf / GPU / Display / Services), text + icons, color-coded.
- Switching performance modes via main window slider updates the gadget's Perf badge live.
- Strip toggle in "G-Scope Floating Settings" hides/shows the strip without restart.
- Tile values + colors persist their existing behavior.
- Window title in settings reads "G-Scope Floating Settings".

**G-Scope panel:**
- Header reads "G-Scope" with help (?) and gear (⚙) buttons.
- Gear opens "G-Scope Settings" window.
- All 8 visible-tile toggles work independently.
- Compact/Regular/Large size presets resize sparklines.
- Multi accent restores per-tile colors; single accents tint everything.
- 60s/5min/15min history window selector visibly changes how much sample data renders.
- Settings persist across app restart.

**Independence:**
- Turn off CPU Fan tile in G-Scope settings → only the panel hides it. Gadget still shows it (unless you also turned it off in gadget settings).
- Set Scope accent to Blue and Gadget accent to Multi → panel is blue, gadget retains per-tile colors.

If anything is off, file the issue and address it before merging.

- [ ] **Step 2: Final commit (only if Task 14 surfaced fixes)**

If the integration test passed cleanly with no follow-up edits, no commit is needed — Task 14 is purely verification. If you had to nudge something, commit the fix with a focused message.
